# Compiler files
cache/
out/

# Ignores development broadcast logs
!/broadcast
/broadcast/*/31337/
/broadcast/**/dry-run/

# Docs
docs/

# Dotenv file
.env

# forge-ctf is copied during local build (from repo root)
# Don't commit it - it's managed by the build process
lib/forge-ctf/

# forge-std is installed at build time via forge install
# Don't commit it - it's managed by the build process
lib/forge-std/
